<div class="box" id="{{id}}">
    <div class="box-header">
        <h2>{{title}}</h2>
        <p>Posted by {{user.username}} on {{created_date}}</p>
    </div>
    <div class="box-content">
        {{content}}
    </div>
</div>